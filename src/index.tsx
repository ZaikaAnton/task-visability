import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

// const App: React.FC = () => {
//   return <h1>Hello, Rollup + React + TypeScript!</h1>;
// };

ReactDOM.render(<App />, document.getElementById("root"));
